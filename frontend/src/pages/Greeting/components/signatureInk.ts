import signatureAndongcheol from '../../../assets/signature-andongcheol.png'

/* 손글씨 사인 — 이름별 잉크 이미지(투명 PNG, 검정 글리프).
   CSS 마스크로 그리므로 색은 소비처의 토큰을 따른다. */
export const SIGNATURE_INK: Record<string, string> = { 안동철: signatureAndongcheol }
